import { Crosshair, Search } from "lucide-react";
import type { FormEvent } from "react";
export function SearchBar({ query, location, compact, onQueryChange, onSubmit, onLocation }: { query: string; location: string; compact?: boolean; onQueryChange: (value: string) => void; onSubmit: () => void; onLocation: () => void; }) { const submit = (event: FormEvent) => { event.preventDefault(); if (query.trim()) onSubmit(); }; return <form className={compact ? "search-bar" : "search-bar search-bar--hero"} onSubmit={submit}><label className="search-field"><Search size={22} /><span className="sr-only">Search for a product</span><input value={query} onChange={(event) => onQueryChange(event.target.value)} placeholder="Search any product" autoComplete="off" /></label>{compact && <button className="search-location" type="button" onClick={onLocation}><Crosshair size={19} /><span>{location}</span></button>}<button className="primary-button search-submit" type="submit">Search</button></form>; }
